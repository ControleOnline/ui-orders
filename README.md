# UI Orders

Módulo compartilhado de pedidos, PDV, checkout, caixa e operação de vendas do ERP.

O fluxo `single-item` mantém a seleção e a substituição do item em `ProductTotem`; o catálogo visual permanece sob responsabilidade do `ui-products`.
