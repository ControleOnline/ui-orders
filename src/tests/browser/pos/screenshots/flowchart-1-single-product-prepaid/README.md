# Flowchart 1 — single product prepaid

Prints e `manifest.json` gerados pelo smoke
`flowchart-1-single-product-prepaid.spec.js`.

- `flowchartIds: [1]`
- `fluxo: compra-fluxo`
- Produção/Ready no mesmo manifesto (`productionSteps`).
